<%= form_for(lack) do |f| %>
  <% if lack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lack.errors.count, "error") %> prohibited this lack from being saved:</h2>

      <ul>
      <% lack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tipodefalta %>
    <%= f.text_field :tipodefalta %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
